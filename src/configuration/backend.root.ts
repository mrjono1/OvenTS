import { Application } from "interfaces";

export const backend: Application = {
  title: 'OvenTS',
  name: 'oven-backend',
  description: "The API Backend to OvenTS",
  outputDirectory: "../output"
};
